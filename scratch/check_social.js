import * as lucide from 'lucide-react';
console.log('Github:', Object.keys(lucide).filter(name => name.toLowerCase().includes('github')));
console.log('Twitter:', Object.keys(lucide).filter(name => name.toLowerCase().includes('twitter')));
console.log('Linkedin:', Object.keys(lucide).filter(name => name.toLowerCase().includes('linkedin')));
